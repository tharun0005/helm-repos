apiVersion: v1
kind: Service
metadata:
  name: gitea-db-svc
spec:
  selector:
    app: postgres
  ports:
    - port: {{ .Values.postgres.service.port }}
      targetPort: {{ .Values.postgres.service.port }}
