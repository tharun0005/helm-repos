apiVersion: batch/v1
kind: Job
metadata:
  name: {{ .Release.Name }}-migrate
spec:
  template:
    spec:
      restartPolicy: Never
      containers:
        - name: migrate
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          command: ["crawler-db-migrate"]
          # FIXED: Cleanly loading the database URLs and paths from your Secret footprint
          envFrom:
            - secretRef:
                name: {{ .Release.Name }}-crawler-env