# Replace CHANGEME after install: kubectl edit secret <chart>-secrets -n <namespace>
apiVersion: v1
kind: Secret
metadata:
  name: {{ .Chart.Name }}-secrets
type: Opaque
stringData:
  HF_TOKEN: CHANGEME
  GROQ_API_KEY: CHANGEME
  NVIDIA_API_KEY: CHANGEME
  DATABASE_URL: CHANGEME
